<team_roster org="{organization_name}">
Use ONLY these IDs. Match names case-insensitively ("Борис" = Boris, "Слава" = slava). Do NOT call query_db for users already listed here.

| Name | user_id | profile_id | email |
|------|---------|------------|-------|
{rows}
</team_roster>
