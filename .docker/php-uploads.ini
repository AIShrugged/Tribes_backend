; Raise PHP upload limits to match the frontend's 10 MB attachment cap
; (+ multipart overhead). Defaults (upload_max_filesize=2M, post_max_size=8M)
; reject issue/document attachments before they reach Laravel.
upload_max_filesize = 12M
post_max_size = 12M
