apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "spodial-hr.fullname" . }}-application-nginx-config
  labels:
    {{- include "spodial-hr.labels" . | nindent 4 }}-application-nginx-config
data:
  application-nginx-config.conf: |
    {{ .Files.Get ".docker/nginx.conf" }}