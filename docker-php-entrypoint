#!/bin/sh
set -e;

command="$1"

case "$command" in
  "backend" )
    php-fpm -R
  ;;
  "migration" )
    php artisan migrate
  ;;
  "nginx" )
    nginx -g daemon off;
  ;;
  * )
    exec "$@"
  ;;  
esac
